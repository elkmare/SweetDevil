// import React, { Component } from 'react';
/* export class SDNav extends Component {
  render() {
    const SDNavPages = ['Identity', 'About', 'Commands', 'Noble Phantasm', 'Skills'];
    const SDNavLinks = SDNavPages.map(page => {
    return (
      <a href={'/' + SDNavPages}>
      {page}
      </a>
    )});
    return
    <nav>{SDNavLinks}</nav>;
  }
} 